[defaults]
# Specify the inventory file location
inventory = inventory.ini

# Set the default remote user for SSH connections
remote_user = monitor

# Specify the default SSH private key file (user can adjust if needed)
private_key_file = ~/.ssh/id_rsa

# Disable host key checking for initial development (consider enabling in production)
host_key_checking = False

# Disable deprecation warnings to reduce noise during development
deprecation_warnings = False

# Disable creation of .retry files for cleaner workspace
retry_files_enabled = False

# Optimize fact gathering for idempotent operations
gathering = smart

# Enable in-memory fact caching for improved performance
fact_caching = memory
fact_caching_timeout = 3600

# Use YAML callback for more readable output
stdout_callback = yaml

# Increase the default timeout for long-running operations
timeout = 30

# Privilege escalation settings
become = True
become_method = sudo
# become_ask_pass = True  # Commented out as we're using vault now

# Vault password file reference
vault_password_file = .vault_password

[ssh_connection]
# Optimize SSH connections for better performance
ssh_args = -o ControlMaster=auto -o ControlPersist=60s

# Enable SSH pipelining for faster execution
pipelining = True

# Set the directory for control socket files
control_path_dir = ~/.ansible/cp

[persistent_connection]
# Set connection timeout and retry attempts
connect_timeout = 30
connect_retries = 3
